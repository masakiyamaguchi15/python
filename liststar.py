# 演習16: リスト要素に応じた数の「*」を出力する

# リストの定義（実行例から数値を抽出）
numbers = [3, 3, 6, 9, 15, 24]

# 合計計算用の変数を初期化
total = 0

# 各要素をループで処理
for num in numbers:
    # 数値とその数だけの「*」を表示
    # Pythonでは 文字列 * 数値 で繰り返し出力が可能です
    print(f"{num}{'*' * num}")
    # 合計を加算
    total += num

# 最終的な合計を表示
print(f"合計： {total}")
